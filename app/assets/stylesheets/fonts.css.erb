<%
  #  Explicitly serve fonts from our own domain, static.gov.uk rather than cdn.
  #  To serve fonts from the cdn (cloudfront) and for it to work on Firefox and IE9+,
  #  they would need to set the "allow origin" header, but they don't support this.
  #  When we have our own cdn, we can solve this ourselves.
  def fonts_url
    if Rails.env.development? || Rails.env.test?
      Plek.current.find('assets') + "/static"
    else
      Plek.current.find('assets') + "/fonts"
    end
  end
%>

@font-face {
  font-family: 'nta';
  src: url(<%= fonts_url %>/nta_light-webfont.eot#) format('embedded-opentype'),
       url(<%= fonts_url %>/nta_light-webfont.woff) format('woff');
  font-weight: normal;
  font-style: normal;
}

@font-face {
  font-family: 'nta-bold';
  src: url(<%= fonts_url %>/nta_medium-webfont.eot#) format('embedded-opentype'),
       url(<%= fonts_url %>/nta_medium-webfont.woff) format('woff');
  font-weight: normal;
  font-style: normal;
}
